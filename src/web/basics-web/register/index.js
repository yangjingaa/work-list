import React ,{Component} from 'react'

import styles from './index.less'

class Register extends Component{
    state={
    };

    render() {
        return (
            <div>
                <p className={styles.errColor}>Register</p>
            </div>
        );
    }

}

export default Register;